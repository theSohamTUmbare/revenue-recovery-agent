# Optional. Without it the agent runs on its deterministic fallback reasoner
# and the demo still works end to end. See README "Running without an API key".
ANTHROPIC_API_KEY=sk-ant-...
RRE_LLM_PROVIDER=anthropic   # anthropic | offline
RRE_LLM_MODEL=claude-sonnet-5
